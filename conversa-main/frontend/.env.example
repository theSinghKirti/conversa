# Public backend URL used by browser HTTP requests.
VITE_API_URL=http://localhost:5500

# Optional. Use when Socket.IO is hosted at a different URL than HTTP API.
VITE_SOCKET_URL=http://localhost:5500
